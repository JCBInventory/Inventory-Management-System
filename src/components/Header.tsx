import React from "react";

const Header: React.FC = () => {
  return (
    <div
      style={{
        background: "#222",
        color: "white",
        padding: "15px",
        fontSize: "20px",
        fontWeight: "bold",
      }}
    >
      JCB Inventory Management System
    </div>
  );
};

export default Header;
